/**
 * Uncomment this file and add env to github when I create and configure "X" account
 */

// const { TwitterApi } = require('twitter-api-v2');
// import 'dotenv/config';

// const version = process.argv[2];
// const url = `${process.env.SITE}/${process.env.BLOG}${process.env.RELEASES}/v${version}`;
// const tweet = `🚀 New blog release v${version} is live! Check out what’s new: ${url}`;

// const client = new TwitterApi({
//   appKey: process.env.X_API_KEY,
//   appSecret: process.env.X_API_SECRET,
//   accessToken: process.env.X_ACCESS_TOKEN,
//   accessSecret: process.env.X_ACCESS_SECRET,
// });

// client.v2.tweet(tweet).then(() => console.log('Tweeted:', tweet));
