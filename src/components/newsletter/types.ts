export type FormStatus = 'default' | 'loading' | 'success' | 'error';

export type Prefs = {
  heaven: boolean;
  announcements: boolean;
  community: boolean;
  author: boolean;
  events: boolean;
  releases: boolean;
};
